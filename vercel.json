{
  "buildCommand": "cd nextapp && npm install && npm run build",
  "outputDirectory": "nextapp/.next",
  "installCommand": "cd nextapp && npm install",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
